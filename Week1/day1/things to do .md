create day1 folder
use below commands

uv init day1
creates a folder and initialise it

cd day1
uv venv --python 3.11
adds python to the folder

uv add groq python-dotenv
groq(llm) and to load environment variables dotenv

always store your api keys in env variable they are cruicial and needs to be protected.
